{#
/**
 * @file
 * Theme override to display a menu.
 *
 * Available variables:
 * - menu_name: The machine name of the menu.
 * - items: A nested list of menu items. Each menu item contains:
 *   - attributes: HTML attributes for the menu item.
 *   - content.children: The menu item child items.
 *   - title: The menu link title.
 *   - url: The menu link url, instance of \Drupal\Core\Url
 *   - content: The field item content.
 *   - localized_options: Menu link localized options.
 *   - is_expanded: TRUE if the link has visible children within the current
 *     menu tree.
 *   - is_collapsed: TRUE if the link has children within the current menu tree
 *     that are not currently visible.
 *   - in_active_trail: TRUE if the link is in the active trail.
 */
#}

<ul class="menu menu-extras list-unstyled menu-level-{{ items|first.menu_level }}">
  {% for key, item in items %}
    {% if key|first != '#' %}
      {% set itemurl %}{{- item.url -}}{% endset %}
      {% set menu_item_classes = [
        'menu-item',
        item.is_expanded ? 'menu-item--expanded',
        item.is_collapsed ? 'menu-item--collapsed',
        item.in_active_trail ? 'menu-item--active-trail',
      ] %}

        {% if itemurl is not empty %}
          <li{{ item.attributes.addClass(menu_item_classes) }}>
            <a href="{{ itemurl }}" class="menu-extras-link" 
               role="button" aria-haspopup="true" 
               title="Menu item: {{ item.title }}" aria-label="Go to {{ item.title }} page">
              <span>{{ item.title }}</span>
            </a>

            {% set rendered_content = item.content|without('') %}
            {% if rendered_content|render %}
              {{ rendered_content }}
            {% endif %}
          </li>
        {% else %}
          <li{{ item.attributes.addClass(menu_item_classes) }}>
            {{ link(item.title, item.url, { 'class':['menu-extras-link'] }) }}
          </li>
         {% endif %}
    {% endif %}
  {% endfor %}
</ul>
